# dbt
dbt/target/
dbt/dbt_packages/
dbt/logs/
dbt/.user.yml

# Local data
data/
context/

# Cursor stores project plans under the user profile (e.g. ~/.cursor/plans); do not duplicate them in-repo
docs/plans/

# Terraform (local state / secrets; commit .terraform.lock.hcl after init)
terraform/**/.terraform/
terraform/**/*.tfstate
terraform/**/*.tfstate.*
terraform/**/backend.hcl
terraform/**/terraform.tfvars
!terraform/**/*.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
build/
dist/
.eggs/
venv/
.env
.env.local
.DS_Store

